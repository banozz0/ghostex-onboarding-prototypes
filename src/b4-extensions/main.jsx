// PROTOTYPE B4 · extensions onboarding. Fork of B3: the switches are back on the tiles, and every extension is
// previewed on the right side of its own panel instead of in a popup.
import { createRoot } from "react-dom/client";
import App from "./App.jsx";

createRoot(document.getElementById("root")).render(<App />);
